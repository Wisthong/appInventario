import { FechaBajaPipe } from './fecha-baja.pipe';

describe('FechaBajaPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaBajaPipe();
    expect(pipe).toBeTruthy();
  });
});
